import CartWidget from './CartWidget';

const NavBar = () => {

    return (
        <nav>
            <a href="">Productos</a>
            <a href="">Servicios</a>
            <CartWidget />
        </nav>
    )
}

export default NavBar